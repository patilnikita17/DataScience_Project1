3️⃣ Create Virtual Environment (Highly Recommended)

Open VS Code terminal:

Ctrl + `


Run:

python -m venv venv


Activate it:

Windows (you are on Windows)
venv\Scripts\activate


You should see:

(venv)


in terminal — this means correct environment.

4️⃣ Select Python Interpreter (VERY IMPORTANT)

Press:

Ctrl + Shift + P


Type:

Python: Select Interpreter


Choose:

Python 3.x (venv)


This ensures:

Kernel works

Libraries install correctly

No hidden errors

📦 STEP 1: Install Required Libraries (5 minutes)

In the same terminal (venv activated):

pip install numpy pandas matplotlib seaborn scikit-learn jupyter joblib


Verify:

pip list